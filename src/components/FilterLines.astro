{/* Three centred bars, each shorter than the one above it — the filter mark
    reduced to what it means. Square viewBox so the glyph sits in the middle of
    a round pill rather than filling it edge to edge. */}
<svg
  class="filter-lines"
  xmlns="http://www.w3.org/2000/svg"
  fill="none"
  viewBox="0 0 24 24"
  stroke-width="2"
  stroke="currentColor"
  stroke-linecap="round"
  aria-hidden="true"
  focusable="false"
>
  <path d="M4 7h16"></path>
  <path d="M7 12h10"></path>
  <path d="M10 17h4"></path>
</svg>
